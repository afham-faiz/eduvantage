{
  "name": "@eduvantage/design-tokens",
  "private": true,
  "version": "0.1.0",
  "description": "Placeholder design token structure. Real brand/design system to be defined by Ailam — do not treat any value here as final.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "devDependencies": {
    "@eduvantage/config": "workspace:*",
    "eslint": "^9.12.0",
    "typescript": "~6.0.3"
  }
}
