// Placeholder for shared test helpers (factories, fixtures, custom matchers).
// Intentionally empty until a real, repeated need shows up across packages —
// see CLAUDE.md on avoiding premature abstraction.
export const EDUVANTAGE_TESTING_PLACEHOLDER = true;
