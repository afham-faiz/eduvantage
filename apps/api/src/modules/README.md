# Future business modules

Real product modules (deals, students, merchants, redemption, admin, …) go
here — one folder per module, each with its own `*.module.ts`, controllers,
services, and DTOs, registered in `../app.module.ts`.

Nothing lives here yet. Foundation 001 is intentionally infrastructure-only.
