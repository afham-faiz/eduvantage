import { registerRootComponent } from "expo";
import App from "./App";

// registerRootComponent handles both the Expo Go / dev-client entry point
// and a standalone native build's entry point.
registerRootComponent(App);
