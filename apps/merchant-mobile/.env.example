# Copy to .env.local for local development (loaded via EXPO_PUBLIC_ prefix).
# No /v1 suffix here — the generated client's paths already include it
# (e.g. client.GET("/v1/deals")), matching the OpenAPI schema exactly.
EXPO_PUBLIC_API_URL=http://localhost:3001
